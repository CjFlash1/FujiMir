# Руководство Администратора

Это руководство описывает функциональность Панели Администратора Fujimir, уделяя особое внимание Конструктору Страниц (Page Builder) и управлению контентом.

## Управление Контентом (Помощь и CMS Страницы)

Панель администратора позволяет управлять Статьями Помощи и CMS Страницами с полной многоязычной поддержкой (Украинский, Русский, Английский).

### Поддержка Языков
Каждая статья или страница имеет три языковые вкладки:
- **🇺🇦 UK** (Украинский) - Основной/По умолчанию
- **🇷🇺 RU** (Русский)
- **🇬🇧 EN** (Английский)

Вы можете переключаться между этими вкладками для независимого редактирования контента на каждом языке.

### Режимы Редактора
Для каждого языка вы можете выбрать один из двух режимов редактирования:
1. **Code / Classic**: Стандартный визуальный редактор (Rich Text) для простого текста и HTML.
2. **✨ Page Builder**: Блочный редактор (drag-and-drop) для создания сложных адаптивных макетов без написания кода.

---

## Функциональность Конструктора Страниц (Page Builder)

Конструктор страниц позволяет собирать страницы из готовых модульных блоков. Чтобы использовать его, нажмите кнопку **"Page Builder"** в редакторе контента.

### Доступные Блоки

Используйте панель инструментов внизу Конструктора Страниц для добавления новых секций.

#### 1. Hero Section (Главная Секция)
Создает крупный, заметный заголовок.
- **Main Title**: Основной заголовок страницы (стиль H1/H2).
- **Subtitle**: Краткое вводное описание под заголовком.

#### 2. Image + Text (Изображение + Текст)
Двухколоночный макет с изображением с одной стороны и текстом с другой.
- **Image URL**: Путь к изображению (например, `/images/help/my-image.jpg`).
- **Image Position**: Выберите **Left** (Слева) или **Right** (Справа).
- **Section Title**: Заголовок текстовой части.
- **Content**: Текстовое описание.

#### 3. Features Grid (Сетка Преимуществ)
Отображает сетку элементов "преимуществ", полезна для перечисления ключевых моментов.
- **Section Title**: Заголовок списка.
- **Features List**: Добавьте несколько текстовых элементов. Они будут отображены в виде сетки с иконками галочек.

#### 4. Data Table (Таблица Данных)
Создание адаптивных таблиц для цен, размеров или характеристик.
- **Table Title**: Необязательный заголовок над таблицей.
- **Data (CSV Format)**: Введите данные в формате CSV (значения, разделенные запятыми).
    - **Первая строка**: Заголовок (определяет названия столбцов).
    - **Последующие строки**: Строки данных.
    
    *Пример:*
    ```csv
    Товар, Цена, Описание
    Фото 10x15, 5 грн, Глянцевая бумага
    Фото A4, 25 грн, Матовая бумага
    Холст 50x50, 400 грн, Деревянный подрамник
    ```

#### 5. Rich Text (Текст)
Стандартный текстовый блок для абзацев, списков и базового форматирования.

### Управление Блоками
- **Изменение порядка**: Используйте **Стрелки Вверх/Вниз** в правом верхнем углу каждого блока.
- **Удаление**: Используйте **Иконку Корзины** (Красную) для удаления блока.

---

## Интеграция с Новой Почтой и Логистика

Система глубоко интегрирована с API Новой Почты (v2.0) для автоматизации создания транспортных накладных (ТТН).

### 1. Настройка API Ключа
Ключ API Новой Почты теперь управляется через общие настройки, что позволяет менять отправителя без вмешательства программиста.
1. Перейдите в раздел **"Налаштування"** (Настройки).
2. Прокрутите вниз до блока **"Інтеграції та API"**.
3. Введите ваш 32-значный ключ API Новой Почты.
4. Нажмите кнопку сохранения 💾.

> **Важно:** Используйте ключ от того аккаунта (бизнес или частного), от имени которого физически будет происходить отправка. Все создаваемые ТТН будут привязаны к этому кабинету.

### 2. Создание ТТН
В деталях заказа (для заказов с доставкой "Новая Почта") нажмите кнопку **"Створити ТТН"**.

#### А. Данные Отправителя (Sender)
Вы можете создавать и сохранять несколько профилей отправителей (например, разные склады или разные люди):
- **Создание**: Заполните Фамилию, Имя, Телефон и выберите Город/Отделение отправки. Нажмите **"Зберегти відправника"**.
- **Выбор**: Используйте выпадающий список сверху блока "Дані відправника". В списке отображается Имя, Телефон и Город склада, чтобы легко различать профили.
- **Редактирование/Удаление**: Выберите профиль из списка. Чтобы удалить его, нажмите кнопку **"Видалити"** (красная корзина).

#### Б. Данные Получателя (Recipient)
Работа с получателем теперь **полностью автоматизирована**:
1. **Авто-режим**: Вам не нужно ничего искать вручную. Просто проверьте, что имя и телефон в заказе верны, и нажмите **"Сформувати ТТН"**.
   - Система сама проверит, существует ли такой клиент в базе НП.
   - Если нет — **автоматически создаст** его.
   - Если есть — выберет его.
   - Если существует несколько людей с таким телефоном, система выберет того, чье Имя и Фамилия совпадают с заказом.
2. **Ручной поиск (Лупа)**: Нажмите на иконку поиска возле телефона, чтобы увидеть список всех контактов, привязанных к этому номеру (на случай, если нужно выбрать специфического получателя вручную).

#### В. Параметры посылки и Время
- **Время**: Если вы создаете ТТН после **18:00**, дата отправки автоматически устанавливается на **завтрашний день**. Это предотвращает ошибки API.
- **Оплата**: Выберите плательщика (Отправитель/Получатель). По умолчанию — Получатель.

### 3. Управление ТТН
После генерации:
- **Номер накладной**: Отображается в блоке доставки.
- **Трекинг**: Ссылка **"Відстежити"** открывает страницу отслеживания НП.
- **Удаление**: Иконка **"X"** позволяет удалить ТТН.
  - *Примечание:* Удалить ТТН можно только до момента сканирования посылки в отделении. Если посылка уже принята, удаление невозможно.

---

## Технические Примечания

- Page Builder генерирует чистый, адаптивный HTML, совместимый с классами Tailwind CSS.
- Изображения должны быть загружены на сервер или размещены на внешнем хостинге и добавлены по ссылке (URL).
- При переключении обратно в режим "Simple" из "Page Builder", сгенерированный HTML сохраняется, но его может быть сложнее редактировать вручную.

---

## Ограничения и Защита (Лимиты)

Система имеет встроенную защиту от спама и перегрузок. На текущий момент действуют следующие лимиты:

1. **Загрузка фото** (`/api/upload`):
   - **Без ограничений**. Клиенты могут загружать любое количество фотографий.

2. **Создание заказов** (`/api/orders`):
   - **10 запросов в минуту** с одного IP. Защита от спама заказами.

3. **Авторизация** (`/api/auth`):
   - **5 запросов в минуту**. Защита от перебора паролей.

4. **Остальные операции**:
   - **60 запросов в минуту**. Общее ограничение для прочих действий API.

> Ограничения распространяются только на "тяжелые" запросы (создание, изменение, удаление данных). Просмотр страниц не ограничен.

---

## Очистка Хранилища Файлов

На странице **Заказы** в правом верхнем углу отображается информация о размере хранилища и кнопка **"Очистка"**.

### Что показывается
- **Размер хранилища**: Общий объём загруженных файлов (включая оригиналы, миниатюры и временные файлы).
- **Количество файлов**: Сколько файлов находится в системе.

### Что делает кнопка "Очистка"
При нажатии запускается процесс проверки и очистки:

1. **Временные файлы** (папка `temp/`):
   - Удаляются чанки незавершённых загрузок старше **1 часа**.
   
2. **Миниатюры** (папка `thumb/`):
   - Удаляются миниатюры, для которых не существует оригинал.
   
3. **Потерянные файлы ("сироты")**:
   - Сканируются файлы, которые не привязаны ни к одному заказу.
   - **Защита Draft-заказов**: Файлы младше **24 часов не трогаются** — это защищает активные сессии клиентов, которые ещё оформляют заказ.
   - Найденные сироты **НЕ удаляются**, а перемещаются в специальный заказ с именем клиента **"SYSTEM RECOVERY"** и статусом **"На удержании"**.

### Как обрабатывать заказы SYSTEM RECOVERY
1. Откройте заказ в админке.
2. Просмотрите прикреплённые файлы.
3. Если файлы нужны — привяжите их к реальному заказу или свяжитесь с клиентом.
4. Если не нужны — удалите заказ (файлы удалятся вместе с ним).

### Автоматические гарантии
- **Draft-заказы** (статус "Оформляется") полностью защищены — их файлы записаны в базу данных как `OrderItem.files` и не считаются сиротами.
- **Активные сессии** пользователей защищены 24-часовым буфером.
- **При удалении заказа** автоматически удаляются связанные файлы и миниатюры.

---

## Технические Примечания (продолжение)

### Папки хранилища
- `public/uploads/` — оригиналы загруженных изображений (конвертируются в JPEG).
- `public/uploads/thumb/` — миниатюры 300×300px для быстрого отображения в UI.
- `public/uploads/temp/` — временные чанки для больших файлов (>2MB).
- `public/uploads/{orderNumber}/` — файлы завершённых заказов (организованы по номеру заказа).

